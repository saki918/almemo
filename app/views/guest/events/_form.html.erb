<%= form_for (event) do |f| %>
  <%= f.label :"ジャンル" %><br>
  <%= f.radio_button :genre_id, id: 0 %>
  <%= image_tag "meal.png", id: 0, width: "30px" %>
  <%= f.collection_select(:genre_id, Genre.all, :id, :name) do |b| %>
  <%= b.label {b.select + b.name} %>
<% end %>
  <%= f.label :"メンバー" %>
  <%
=begin%>
  f.collection.select :データを保存させたい先のID(pk),保存する情報をどこから持ってくるかのモデル先,取ってきたモデルのどのIDをpkに入れるかの指定(fk),表示させたいカラム名
<%
=end%>
  <%= f.collection_select :eventmember_id, current_guest.members,:id,:name, multiple: true %><br>
  <%= f.label :"タイトル" %>
  <%= f.text_field :title %><br>
  <%= f.label :"開始時間" %>
  <%= f.datetime_select :start_time %><br>
  <%= f.label :"終了時間" %>
  <%= f.datetime_select :finish_time %><br>
  <%= f.label :"コメント" %>
  <%= f.text_area :comment %><br>
  <%= f.label :"画像" %>
  <%= f.file_field :image, multiple: true %><br>
  <%= f.submit "イベント作成", class: "btn btn-primary" %>
<% end %>